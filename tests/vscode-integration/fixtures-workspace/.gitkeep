# Empty workspace folder opened by @vscode/test-electron during Layer-3 runs.
# Required so `vscode.workspace.workspaceFolders` is defined inside tests
# (some tests expect a workspace root for path resolution).
