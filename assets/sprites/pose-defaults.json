{
  "_note": "Pose-keyed playback DEFAULTS (anim-playback epic E3, 86ca2187n). Keyed by canonical anim name, this block sets playback fields ONCE for ALL characters — e.g. `idle_stretch: { playbackMode: \"pingpong\", finalDwellMs: 800 }` makes every character's stretch ping-pong without a per-char edit. The cascade is 3-LAYER and FIELD-LEVEL: a character's own animations.json `playback.<anim>.<field>` wins; else this pose-default's `<field>`; else the engine default (the absent-field behavior in spritePlayer.ts). The merge is per-FIELD, not whole-object — a per-char override that sets only `speedMultiplier` still inherits this pose-default's `playbackMode`/`finalDwellMs`. Same field vocabulary + validation as animations.json `playback` (speedMultiplier, dwellFrameIndex, dwellMs, finalDwellMs, playbackMode, startFrame, endFrame); malformed values are dropped + warned by scripts/build-sprite-manifest.mjs. SHIPPED EMPTY: no pose-defaults are seeded — the sponsor seeds e.g. idle_stretch when ready. Empty {} → byte-identical to the E2 end-state (AC3). Edit + `npm run build` + reload to apply, no TS edit.",
  "playback": {}
}
